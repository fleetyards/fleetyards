name: Legacy Deploy Job

on:
  workflow_call:
    inputs:
      destination:
        required: true
        type: string
      name:
        required: true
        type: string
      url:
        required: true
        type: string

jobs:
  deploy:
    runs-on: ubuntu-latest

    environment:
      name: ${{ inputs.name }}
      url: ${{ inputs.url }}

    steps:
      - uses: actions/checkout@v4
      - name: Install SSH key
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.LEGACY_SSH_KEY }}
          known_hosts: unnecessary
          if_key_exists: ignore
      - name: Fetch known hosts
        run: ssh-keyscan fleetyards.mortik.xyz >> ~/.ssh/known_hosts
      - uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true
      - name: Deploy
        run: bundle exec cap ${{ inputs.capEnv }} deploy
