#!/usr/bin/env bash
set -euo pipefail

set +a
export HCLOUD_TOKEN=$(op read "op://Fleetyards/HCLOUD_STAGE/credential")

ROLE="${1:-web}"
shift 2>/dev/null || true
bundle exec kamal app exec -d stage -r "$ROLE" "$@"
