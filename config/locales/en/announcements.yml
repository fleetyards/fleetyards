---
en:
  announcements:
    channels:
      in_app: "In-app"
      discord: "Discord"
      bluesky: "Bluesky"
      x: "X.com"
    deliveries:
      not_configured: "This channel has no credentials configured."
    preview:
      title: "TEST — %{title}"
      channels: "Would go to: %{channels}"
      social_heading: "**%{channel}** (%{count}/%{limit} characters)"
