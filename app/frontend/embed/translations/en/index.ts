import datetime from "@/translations/en/datetime";
import number from "@/translations/en/number";
import model from "@/translations/en/model";
import actions from "./actions";
import labels from "./labels";

export default {
  actions,
  datetime,
  number,
  labels,
  model,
};
