# frozen_string_literal: true

require "test_helper"

class ShipsRoutesTest < ActiveSupport::TestCase
  it "should respond to /ships"
  it "should respond to /ships/:id"
  it "should respond to /ships/reload"
end
