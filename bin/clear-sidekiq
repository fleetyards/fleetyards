#!/usr/bin/env bash

set -eu

echo "Clearing Sidekiq queues..."
bin/rails runner '
require "sidekiq/api"
Sidekiq::Queue.all.each(&:clear)
Sidekiq::RetrySet.new.clear
Sidekiq::ScheduledSet.new.clear
Sidekiq::DeadSet.new.clear
puts "Done!"
'
