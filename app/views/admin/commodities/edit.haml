- content_for :title do
  = I18n.t(:"title.commodity.edit")
.row
  .col-xs-12.col-md-12

    .row
      .col-xs-12.col-md-8
        %h2
          = I18n.t(:"headlines.admin.commodity.edit", commodity: commodity.name)

      .col-xs-12.col-md-4
        .btn-group.pull-right.resource-nav
          = link_to I18n.t("actions.back"), admin_commodities_path(params: index_back_params, anchor: commodity.id), class: "btn btn-default"

    = render "form"
