.table-responsive
  %table.table.table-hover.table-striped
    %thead
      %tr
        %th.preview
        %th.preview
        %th.preview
        %th=sort_link(@q, :name)
        %th.rsi-id=sort_link(@q, :rsi_id, 'RSI ID')
        %th=sort_link(@q, :hidden)
        %th=sort_link(@q, :active)
        %th.actions.actions-four-ladda=I18n.t(:"labels.table.actions")
    %tbody
      = render partial: "admin/models/show", collection: @models, as: :model

      - if @models.blank?
        = render "shared/tables/blank", count: 8, resource: "model"
