#!/usr/bin/env bash
set -euo pipefail

set +a
HCLOUD_TOKEN=$(op read "op://Fleetyards/HCLOUD_STAGE/credential")
export HCLOUD_TOKEN

ROLE="${1:-web}"
shift 2>/dev/null || true
bundle exec kamal app logs -d stage -r "$ROLE" "$@"
