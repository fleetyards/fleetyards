production: &default
  adapter: redis
  url: <%= Rails.configuration.x.redis.url %>
  channel_prefix: <%= Rails.configuration.x.app.cable_channel_prefix %>

docker:
  <<: *default

development:
  <<: *default

test:
  adapter: test
