# == Schema Information
#
# Table name: module_hardpoints
#
#  id              :uuid             not null, primary key
#  created_at      :datetime         not null
#  updated_at      :datetime         not null
#  model_id        :uuid
#  model_module_id :uuid
#
galaxy_cargo_hardpoint:
  model_module: galaxy_cargo
  model: galaxy
