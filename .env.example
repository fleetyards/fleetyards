PORT=8270
DOMAIN=fleetyards.test
SHORT_DOMAIN=fltyrd.test
ON_SUBDOMAIN=true
DB_PORT=8271
DB_PASSWORD=password
REDIS_URL=redis://localhost:8272
MAILER_DEFAULT_FROM=info@fleetyards.test
# == Kamal deployment ==
# Secrets are fetched from 1Password at deploy time.
# Set your service account token before deploying:
#
#   export OP_SERVICE_ACCOUNT_TOKEN="your-token"
#   kamal deploy -d live
#   kamal deploy -d stage
#
# All deployment secrets are stored in the "Fleetyards" vault in 1Password.
# See .kamal/secrets-common for the full list.
