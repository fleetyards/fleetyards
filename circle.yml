dependencies:
  pre:
    - gem update --system
test:
  override:
    - bundle exec rake
    - bundle exec rubocop
deployment:
  live:
    branch: master
    commands:
      - bundle exec mina live deploy
